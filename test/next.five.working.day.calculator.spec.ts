import "mocha";

describe('NEXT FIVE WORKING DAY CALCULATOR', () => {

    it('should throw error when given invalid date input', () => {
    });

    it('should throw error when given empty date input', () => {
    });

    it('should throw error when given date in the past', () => {
    });

    it('should exclude holidays when calculating next five working days', () => {
    });

    it('should exclude weekends when calculating next five working days', () => {
    });
});
